-- [[ SABER SIMULATOR ULTRA HUB | FULL FIXED VERSION ]] --

if not game:IsLoaded() then game.Loaded:Wait() end

-- ============================================
-- 1. ГЛОБАЛЬНЫЙ КОНФИГ
-- ============================================
getgenv().SaberConfig = {
    AutoSwing = false,
    AutoSell = false,
    AutoQuest = false,
    DungeonActive = false,
    AutoCrowns = false,
    WalkSpeedValue = 45,
    SelectedEgg = "Wooden Egg",
    SelectedDungeon = "Space",
    SelectedDifficulty = 1,
    DunFarmingDistance = 9,
    -- Флаги для квестов
    skipDungeonQuest = false,
    skipEggQuest = false,
    skipBossQuest = false
}

-- ============================================
-- 2. СЕРВИСЫ
-- ============================================
local Players = game:GetService("Players")
local VirtualInputManager = game:GetService("VirtualInputManager")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local CollectionService = game:GetService("CollectionService")
local VirtualUser = game:GetService("VirtualUser")

local lp = Players.LocalPlayer
local Character = lp.Character or lp.CharacterAdded:Wait()

-- ============================================
-- 3. ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
-- ============================================
function getBossData()
    local gameplay = workspace:FindFirstChild("Gameplay")
    local bossFolder = gameplay and gameplay:FindFirstChild("Boss")
    local holder = bossFolder and bossFolder:FindFirstChild("BossHolder")
    local model = holder and holder:FindFirstChildOfClass("Model")
    if not model then return nil end
    return model:FindFirstChild("HumanoidRootPart")
end

-- Хук на урон (Hit Spoof)
pcall(function()
    local HitDetection = require(lp.PlayerScripts.MainClient.ClientTool.HitDetection)
    if not getgenv().oldGetHits then getgenv().oldGetHits = HitDetection.GetBossesAndMobsHit end
    HitDetection.GetBossesAndMobsHit = function(self, player)
        if getgenv().SaberConfig.AutoQuest or getgenv().SaberConfig.DungeonActive then
            local hitAll = {}
            local seen = {}
            for _, tag in ipairs({"Boss", "EventBoss", "Mob", "DungeonEnemy"}) do
                for _, mob in ipairs(CollectionService:GetTagged(tag)) do
                    if mob and mob.Parent and not seen[mob] then
                        local hrp = mob:FindFirstChild("HumanoidRootPart")
                        if hrp and (mob:GetAttribute("Health") or 1) > 0 then
                            seen[mob] = true
                            table.insert(hitAll, mob)
                        end
                    end
                end
            end
            return hitAll
        end
        return getgenv().oldGetHits(self, player)
    end
end)

-- ============================================
-- 4. ФОНОВЫЕ ЦИКЛЫ (ВКЛЮЧАЮТСЯ КНОПКАМИ)
-- ============================================

-- Взмахи
task.spawn(function()
    local clientTool = require(lp.PlayerScripts.MainClient.ClientTool)
    while true do
        if getgenv().SaberConfig.AutoSwing and lp.Character then
            local tool = lp.Character:FindFirstChildOfClass("Tool")
            if tool and tool:FindFirstChild("RemoteClick") then
                clientTool:Swing()
            end
        end
        task.wait(0.05)
    end
end)

-- Продажа
task.spawn(function()
    while true do
        if getgenv().SaberConfig.AutoSell then
            ReplicatedStorage.Events.SellStrength:FireServer()
        end
        task.wait(0.5)
    end
end)

-- Сбор корон
task.spawn(function()
    while true do
        if getgenv().SaberConfig.AutoCrowns and lp.Character then
            local root = lp.Character:FindFirstChild("HumanoidRootPart")
            if root then
                pcall(function()
                    for _, pickup in pairs(workspace.Gameplay.CurrencyPickup.CurrencyHolder:GetChildren()) do
                        if pickup.Transparency == 0 and pickup.Name == "Crown" then
                            pickup.CFrame = root.CFrame
                            pickup.Anchored = true
                        end
                    end
                end)
            end
        end
        task.wait(0.3)
    end
end)

-- Анти-АФК
lp.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new(0,0))
end)

-- ============================================
-- 5. ТВОЯ ОЧИЩЕННАЯ ЛОГИКА (КВЕСТЫ И ДАНЖИ)
-- ============================================

-- Функция авто-входа в данж
function autoJoinDungeon()
    pcall(function()
        local dungeonStorage = workspace:FindFirstChild("DungeonStorage")
        local isInDungeon = false
        if dungeonStorage then
            for _, folder in pairs(dungeonStorage:GetChildren()) do
                if #folder:GetChildren() > 0 then isInDungeon = true; break end
            end
        end
        if not isInDungeon then
            local dataManager = require(lp.PlayerScripts.MainClient.ClientDataManager)
            local dateTimeManager = require(lp.PlayerScripts.MainClient.DateTimeManager)
            if dateTimeManager:Now() >= (dataManager.Data.DungeonCooldownEndDT or 0) then
                ReplicatedStorage.Events.UIAction:FireServer("DungeonGroupAction", "Create", "Public", getgenv().SaberConfig.SelectedDungeon, getgenv().SaberConfig.SelectedDifficulty)
                task.wait(1.5)
                ReplicatedStorage.Events.UIAction:FireServer("DungeonGroupAction", "Start")
            end
        end
    end)
end

-- Функция цикла квестов (Очищена)
function startAutoQuestLoop()
    while getgenv().SaberConfig.AutoQuest do
        pcall(function()
            local ClientDataManager = require(lp.PlayerScripts.MainClient.ClientDataManager)
            local QuestInfo = require(ReplicatedStorage.Modules.QuestInfo)
            local quests = ClientDataManager.Data and ClientDataManager.Data.ClanQuests
            if not quests then return end

            for questIndex, q in pairs(quests) do
                local cfg = QuestInfo.ClanQuests[q.Id]
                if cfg and q.Amount >= cfg.GoalAmount then
                    ReplicatedStorage.Events.UIAction:FireServer("ClaimClanQuest", questIndex)
                    task.wait(1)
                end
            end
            -- Тут будет логика фарма (BossWalk, EggFarm и т.д.)
        end)
        task.wait(1)
    end
end

-- ============================================
-- 6. ИНТЕРФЕЙС (GUI)
-- ============================================
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "Saber Simulator | Ultra Hub",
   LoadingTitle = "Скрипт загружен!",
   ConfigurationSaving = { Enabled = true, FolderName = "SaberConfig" },
   KeySystem = false
})

local TabMain = Window:CreateTab("🏠 Главное", 4483362458)
local TabFarm = Window:CreateTab("⚔️ Фарм", 4483362458)
local TabDungeon = Window:CreateTab("🏰 Данжи", 4483362458)

-- ВКЛАДКА: ГЛАВНОЕ
TabMain:CreateSection("Автоматизация")
TabMain:CreateToggle({
   Name = "Авто-Взмахи",
   CurrentValue = false,
   Callback = function(v) getgenv().SaberConfig.AutoSwing = v end,
})
TabMain:CreateToggle({
   Name = "Авто-Продажа DNA",
   CurrentValue = false,
   Callback = function(v) getgenv().SaberConfig.AutoSell = v end,
})
TabMain:CreateToggle({
   Name = "Авто-Сбор Корон",
   CurrentValue = false,
   Callback = function(v) getgenv().SaberConfig.AutoCrowns = v end,
})

-- ВКЛАДКА: КВЕСТЫ
TabFarm:CreateSection("Клановые квесты")
TabFarm:CreateToggle({
   Name = "ВЫПОЛНЯТЬ ВСЕ КВЕСТЫ",
   CurrentValue = false,
   Callback = function(v) 
      getgenv().SaberConfig.AutoQuest = v 
      if v then task.spawn(startAutoQuestLoop) end
   end,
})

-- ВКЛАДКА: ДАНЖИ
TabDungeon:CreateSection("Данж Настройки")
TabDungeon:CreateDropdown({
   Name = "Сложность",
   Options = {"1 (Easy)", "2 (Medium)", "3 (Hard)", "4 (Impossible)"},
   CurrentOption = {"1 (Easy)"},
   Callback = function(opt) getgenv().SaberConfig.SelectedDifficulty = tonumber(opt[1]:sub(1,1)) end,
})
TabDungeon:CreateToggle({
   Name = "Авто-Данж (Вход + Цикл)",
   CurrentValue = false,
   Callback = function(v) 
      getgenv().SaberConfig.DungeonActive = v 
      if v then 
         task.spawn(function()
            while getgenv().SaberConfig.DungeonActive do
               autoJoinDungeon()
               task.wait(10)
            end
         end)
      end
   end,
})

Rayfield:Notify({
   Title = "Успешно!",
   Content = "Hub готов к использованию",
   Duration = 5,
   Image = 4483362458,
})
